# Core engine + RL
numpy>=1.26
torch>=2.2          # RL self-play training & policy inference (CPU or CUDA)

# API server
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
email-validator>=2.1
python-multipart>=0.0.9

# Database + auth (Postgres locally via Docker, Neon in production)
SQLAlchemy>=2.0
psycopg2-binary>=2.9
bcrypt>=4.1
PyJWT>=2.8
